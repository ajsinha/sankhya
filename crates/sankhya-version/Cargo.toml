[package]
name         = "sankhya-version"
description  = "The four version axes, and what a reader does with an artifact it did not write."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license-file.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 0

[lints]
workspace = true

[dependencies]
