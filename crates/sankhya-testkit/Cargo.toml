[package]
name         = "sankhya-testkit"
description  = "Deterministic fakes and fault injectors for every seam."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license-file.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 1

[lints]
workspace = true

[dependencies]
sankhya-atomicfs = { path = "../sankhya-atomicfs" }
# Certificates, generated. A private key committed to a repository is a private key,
# whatever the file is named, and a fixture certificate expires on a date nobody chose.
rcgen = { workspace = true }
