[package]
name         = "sankhya-cdc-apply"
description  = "Change-event stream to table-mutation plan. Pure; the main sync testing seam."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license-file.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 1

[lints]
workspace = true

[dependencies]
sankhya-types     = { path = "../sankhya-types" }
sankhya-error     = { path = "../sankhya-error" }
sankhya-cdc-model = { path = "../sankhya-cdc-model" }

[dev-dependencies]
proptest = { workspace = true }
