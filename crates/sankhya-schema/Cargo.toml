[package]
name         = "sankhya-schema"
description  = "Logical schema model, type mapping, mirror naming and automatic onboarding."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license-file.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 0

[lints]
workspace = true

[dependencies]
sankhya-types     = { path = "../sankhya-types" }
sankhya-error     = { path = "../sankhya-error" }
sankhya-cdc-model = { path = "../sankhya-cdc-model" }
arrow-schema      = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
