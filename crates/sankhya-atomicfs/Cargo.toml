[package]
name         = "sankhya-atomicfs"
description  = "Publishing a file all at once, and claiming a name exclusively. No dependencies."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license-file.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 0

[lints]
workspace = true

[dependencies]

[dev-dependencies]
tempfile = { workspace = true }
