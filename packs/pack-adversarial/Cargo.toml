[package]
name         = "pack-adversarial"
description  = "A deliberately hostile pack. Every attempt it makes must be rejected with a named error."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[package.metadata.sankhya]
layer = 99

[lints]
workspace = true

[dependencies]
sankhya-ext = { path = "../../crates/sankhya-ext" }
